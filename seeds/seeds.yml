# seeds/seeds.yml
version: 2
seeds:
  - name: raw_app_events
    config:
      column_types:
        event_timestamp: timestamp
  - name: raw_members
    config:
      column_types:
        created_at: date
  - name: raw_claims
    config:
      column_types:
        claim_date: date
        service_date: date
        claim_amount: numeric
        paid_amount: numeric
